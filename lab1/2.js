// Program to find the largest of three given numbers
function max(x, y, z){
    return Math.max(x, y, z)
}

console.log(`Largest number is: ${max(1, 0, 1)}`);
console.log(`Largest number is: ${max(0, -10, -20)}`);
console.log(`Largest number is: ${max(1000, 510, 440)}`);